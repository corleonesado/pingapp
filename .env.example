# HTTP listen port (default 8080)
PORT=8080

# Log level: debug | info | warn | error (default info)
LOG_LEVEL=info

# VERSION is injected at build time via Docker --build-arg VERSION=<sha>
# and Go -ldflags "-X main.version=<sha>"; it is not read from env at runtime.
